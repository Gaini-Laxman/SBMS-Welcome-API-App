server:
  port: 9091

spring:
  application:
    name: WELCOME API
  boot:
    admin:
      client:
        url: http://localhost:1000/
    
management:
  endpoints:
    web:
      exposure:
        include: '*'